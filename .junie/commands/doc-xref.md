---
description: Find inbound references to a doc path, heading, anchor, or public symbol.
allowPromptArgument: true
---

Follow `.junie/skills/doc-xref/SKILL.md` as the authoritative workflow for this command.
Treat `$prompt` as the required target path, anchor, heading, or symbol to trace.
If `$prompt` is empty, stop and ask for the target rather than picking one yourself.
