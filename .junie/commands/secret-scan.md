---
description: Scan paths or suspicious content for likely secrets using the repository's secret-scan workflow.
allowPromptArgument: true
---

Follow `.junie/skills/secret-scan/SKILL.md` as the authoritative workflow for this command.
Treat `$prompt` as an optional path list or scan focus; if omitted, use the workflow's documented default behavior.
