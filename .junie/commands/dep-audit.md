---
description: Run the dependency audit workflow and summarize actionable findings.
allowPromptArgument: true
---

Follow `.junie/skills/dep-audit/SKILL.md` as the authoritative workflow for this command.
Treat `$prompt` as an optional scope hint such as a path, dependency family, or audit focus.
If no scope is given, audit the standard repository dependency surfaces.
