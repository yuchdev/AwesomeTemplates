---
description: Create or update an ADR from the repository's ADR-writing workflow.
allowPromptArgument: true
---

Follow `.junie/skills/adr-write/SKILL.md` as the authoritative workflow for this command.
Treat `$prompt` as the ADR title or decision topic.
If `$prompt` is empty, stop and ask for the ADR title instead of guessing.
