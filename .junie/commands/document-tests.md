---
description: Document automated tests under a path using the repository's test-doc workflow.
allowPromptArgument: true
---

Follow `.junie/skills/document-tests/SKILL.md` as the authoritative workflow for this command.
Treat `$prompt` as an optional path; default to `tests/` when it is omitted.
