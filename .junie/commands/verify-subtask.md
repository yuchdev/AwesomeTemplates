---
description: Verify a roadmap subtask implementation against its written spec.
allowPromptArgument: true
---

Follow `.junie/skills/verify-subtask/SKILL.md` as the authoritative workflow for this command.
Treat `$prompt` as the required subtask path.
If `$prompt` is empty, stop and ask for the subtask path rather than guessing.
