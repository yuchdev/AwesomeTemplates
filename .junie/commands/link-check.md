---
description: Validate documentation links and anchors for a path or for the whole doc corpus.
allowPromptArgument: true
---

Follow `.junie/skills/link-check/SKILL.md` as the authoritative workflow for this command.
Treat `$prompt` as an optional path list; when omitted, run the documented default whole-doc scope.
