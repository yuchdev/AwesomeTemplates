# 01 - Page skeleton

**Parent story:** [README.md](README.md)
**Status:** ⬜ Not started

## Objective

This task is a stub to rename and implement a real one.
All subsequent paragraphs also should be rewritten into a real task.

## Files

- `index.html` - new page with `<!doctype html>`, `lang`, `charset`, viewport meta, and `<header>`/`<main>`/`<footer>` landmarks.
- `css/main.css` - design-token custom properties and mobile-first base styles.

## Elements and Functions

- A single `<h1>` reading "{{PROJECT_NAME}}" inside `<main>`.

## Constraints

- None

## Success criteria

- [ ] `index.html` validates with no errors under the project's HTML validator.
- [ ] The page has exactly one `<main>` and one `<h1>`.
- [ ] Layout is readable with no horizontal scroll at 375px and 1280px viewport widths.
